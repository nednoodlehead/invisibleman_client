# responsible for duplicating the main.db elsewhere. Either somewhere inside the directory
# or better yet, somewhere like %userprofile%/appdata/backups, so it is harder to lose on accident
# maybe run that on a thread each time something happens !?
# will do later in the application's development
